import { OpButtonDirective } from './op-button.directive';

describe('OpButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new OpButtonDirective();
    expect(directive).toBeTruthy();
  });
});
